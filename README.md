# EREZMonitor

Легковесная утилита мониторинга системных ресурсов для Windows.

## Возможности

- **CPU мониторинг**: общая нагрузка, по ядрам, температура
- **RAM мониторинг**: использование, swap
- **GPU мониторинг**: нагрузка NVIDIA GPU, температура, VRAM (требует NVIDIA драйвер)
- **Диск мониторинг**: скорость чтения/записи, IOPS, использование дисков
- **Сеть мониторинг**: входящий/исходящий трафик
- **Процессы**: топ процессов по CPU и памяти
- **Системный трей**: иконка с цветовой индикацией нагрузки
- **Алерты**: уведомления при превышении порогов
- **Логирование**: запись метрик в CSV
- **Горячие клавиши**: глобальные комбинации клавиш
- **Автозагрузка**: запуск с Windows

## Требования

- Windows 10/11
- Go 1.21+
- NVIDIA GPU и драйвер (опционально, для GPU мониторинга)

## Установка

### Из исходного кода

```powershell
# Клонировать репозиторий
git clone https://github.com/user/erez-monitor.git
cd erez-monitor

# Установить зависимости и собрать
.\build.ps1

# Или для релизной сборки (без консольного окна)
.\build.ps1 -Release
```

### Готовый бинарник

Скачайте последний релиз из [Releases](https://github.com/user/erez-monitor/releases).

## Использование

```powershell
# Обычный запуск
.\EREZMonitor.exe

# Режим отладки
.\EREZMonitor.exe --debug

# Запуск только в трей
.\EREZMonitor.exe --tray-only

# С пользовательским конфигом
.\EREZMonitor.exe --config path/to/config.yaml
```

## Настройка

Конфигурационный файл `config.yaml`:

```yaml
monitoring:
  update_interval: 1s      # Интервал обновления метрик
  history_duration: 60s    # Длительность хранения истории
  enable_gpu: true         # Включить GPU мониторинг
  enable_processes: true   # Включить мониторинг процессов
  top_process_count: 10    # Количество топ процессов

alerts:
  enabled: true
  cpu_threshold: 80        # Порог CPU для алерта (%)
  ram_threshold: 85        # Порог RAM (%)
  gpu_threshold: 85        # Порог GPU (%)
  gpu_temp_threshold: 85   # Порог температуры GPU (°C)
  disk_threshold: 90       # Порог заполнения диска (%)
  cooldown: 30s            # Минимальный интервал между алертами
  sound_enabled: true      # Звуковое уведомление

ui:
  tray_enabled: true
  autostart: false         # Запуск с Windows
  hotkey: "Ctrl+Shift+M"   # Горячая клавиша для окна статистики
  theme: "dark"
  window_width: 800
  window_height: 600
  refresh_rate: 500ms

overlay:
  enabled: false
  position: "top-right"    # Позиция: top-right, top-left, bottom-right, bottom-left
  opacity: 0.8
  font_size: 16
  hotkey: "Ctrl+Shift+O"

logging:
  level: "info"            # debug, info, warn, error
  to_file: true
  csv_export: true
  max_file_size: "10MB"
  rotation: "daily"
```

## Горячие клавиши

- `Ctrl+Shift+M` - показать/скрыть окно статистики
- `Ctrl+Shift+O` - включить/выключить оверлей

## Иконка в трее

Цвет иконки меняется в зависимости от нагрузки:
- 🟢 **Зеленая**: CPU < 50%, RAM < 70%
- 🟡 **Желтая**: CPU 50-80%, RAM 70-85%
- 🔴 **Красная**: CPU > 80% или RAM > 85%

## Меню трея

- **Show Details** - открыть окно статистики
- **Toggle Overlay** - включить/выключить оверлей
- **Settings** - настройки
- **Export Logs** - экспортировать метрики в CSV
- **Start with Windows** - автозагрузка
- **Exit** - выход

## Разработка

### Структура проекта

```
erez-monitor/
├── main.go                 # Entry point
├── config/
│   ├── config.go           # Конфигурация
│   └── config.yaml         # Дефолтный конфиг
├── collector/
│   ├── collector.go        # Главный сборщик
│   ├── cpu.go              # CPU метрики
│   ├── memory.go           # RAM метрики
│   ├── gpu.go              # GPU метрики (NVML)
│   ├── disk.go             # Диск I/O
│   ├── network.go          # Сетевые метрики
│   └── processes.go        # Процессы
├── storage/
│   └── ringbuffer.go       # Кольцевой буфер
├── alerter/
│   └── alerter.go          # Алерты
├── ui/
│   └── tray.go             # Системный трей
├── logger/
│   └── logger.go           # Логирование
├── hotkeys/
│   └── hotkeys.go          # Горячие клавиши
├── autostart/
│   └── autostart.go        # Автозагрузка
├── models/
│   └── metrics.go          # Структуры данных
├── utils/
│   ├── format.go           # Форматирование
│   └── windows.go          # Windows API
├── build.ps1               # PowerShell скрипт сборки
└── Makefile                # Make targets
```

### Запуск тестов

```powershell
go test -v ./...

# С покрытием
go test -v -cover ./...

# Только storage
go test -v ./storage/...
```

### Сборка

```powershell
# Debug сборка
.\build.ps1

# Release сборка (без консоли)
.\build.ps1 -Release

# С UPX сжатием
.\build.ps1 -Release -Compress

# Очистка и сборка
.\build.ps1 -Clean -Release

# Тесты + сборка
.\build.ps1 -Test -Release
```

## Зависимости

- [shirou/gopsutil](https://github.com/shirou/gopsutil) - сбор системных метрик
- [getlantern/systray](https://github.com/getlantern/systray) - системный трей
- [spf13/viper](https://github.com/spf13/viper) - конфигурация
- [sirupsen/logrus](https://github.com/sirupsen/logrus) - логирование
- [natefinch/lumberjack](https://github.com/natefinch/lumberjack) - ротация логов
- [golang.org/x/sys/windows](https://pkg.go.dev/golang.org/x/sys/windows) - Windows API

## Лицензия

MIT License
